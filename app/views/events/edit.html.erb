<% provide(:title, 'Edit: %s' % @event.title) %>

<div class="container">
  <div class="main-content">
    <h1>Editing Event</h1>

    <%= render 'form', event: @event %>

    <%= link_to 'Show', @event %> |
    <%= link_to 'Back', events_path %>
  </div>
</div>
